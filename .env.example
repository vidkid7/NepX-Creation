# Database (PostgreSQL - required for deployment)
# Local: postgresql://postgres:password@localhost:5432/nepx_creation?schema=public
# Production: set in Vercel/Railway/Neon/etc. (e.g. postgresql://user:pass@host:5432/db?sslmode=require)
DATABASE_URL="postgresql://username:password@localhost:5432/nepx_creation?schema=public"

# NextAuth (Railway: set NEXTAUTH_URL to https://YOUR-APP.up.railway.app)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"

# Admin credentials (for initial setup)
ADMIN_EMAIL="admin@nepxcreation.com"
ADMIN_PASSWORD="admin123"
